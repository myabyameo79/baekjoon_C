#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void)
{
	int t, a, b;
	scanf("%d", &t);
	while(t!=0) {
		scanf("%d %d", &a, &b);
		printf("%d", a + b);
		printf("\n");
		t--;
	}
	return 0;
}
